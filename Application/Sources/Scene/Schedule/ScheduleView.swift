import SwiftUI

struct ScheduleView: View {
    var body: some View {
        Text("일정 화면")
    }
}

struct ScheduleView_Previews: PreviewProvider {
    static var previews: some View {
        ScheduleView()
    }
}
